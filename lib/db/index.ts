/**
 * AWS Aurora PostgreSQL — connection utility (stub)
 *
 * Replace this stub with your chosen driver:
 *   - postgres.js  → `import postgres from "postgres"`
 *   - Drizzle ORM  → `import { drizzle } from "drizzle-orm/postgres-js"`
 *   - node-postgres → `import { Pool } from "pg"`
 *
 * Connection string is read from DATABASE_URL in .env.local.
 * Aurora requires sslmode=require — ensure it is in the URL.
 */

export {};
