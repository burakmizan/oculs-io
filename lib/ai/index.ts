/**
 * AI agent orchestration — Gemini integration (stub)
 *
 * Planned capabilities:
 *   - analyzeFindings(rawScanJson)  → structured FindingSummary[]
 *   - generateAutoFix(finding)      → UnifiedDiff patch string
 *   - triageSeverity(finding)       → SeverityScore with reasoning
 *   - generateReport(scanId)        → Markdown security report
 *
 * Uses GEMINI_API_KEY and GEMINI_MODEL from environment.
 */

export {};
